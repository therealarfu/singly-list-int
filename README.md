# singly-list-int
This project is a library with multiple functions that allows to dinamically use a singly list. This code makes manipulating a list of integers much easier, in contrast to the fixed arrays in the C language.
